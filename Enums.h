#pragma once

enum gamePart
{
    MENU,
    GAME,
    GAMEOVER,
    LEADERBOARD,
    EXIT
};

enum pieceEnum
{
    NONE = 0,
    IP,
    JP,
    LP,
    OP,
    SP,
    TP,
    ZP,
    SHADOW,
    WALL
};